<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login - Kanglung Directory</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet"/>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet"/>

  <style>
    body {
      font-family: "Poppins", sans-serif;
      background-color: #000;
      color: #fff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }
    .login-container {
      background: #fff;
      color: #000;
      border-radius: 1.5rem;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.7);
      padding: 2.5rem;
      max-width: 400px;
      width: 100%;
    }
    .logo {
      display: block;
      margin: 0 auto 2rem;
      width: 80px;
      height: 80px;
      border-radius: 9999px;
      background: #000;
      box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.5),
                  0 4px 6px -4px rgba(0, 0, 0, 0.5);
      object-fit: cover;
    }
    .form-label {
      font-weight: 600;
    }
    .form-control:focus {
      box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.3);
      border-color: #000;
    }
    .btn-dark {
      background-color: #000;
      border: none;
    }
    .btn-dark:hover {
      background-color: #333;
    }
    .footer-text {
      text-align: center;
      color: #6c757d;
      font-size: 0.875rem;
      margin-top: 2rem;
    }
  </style>
</head>
<body>

  <div class="login-container" role="main" aria-label="Login form container">
    
  <h2 class="text-center mb-2">Welcome Back</h2>
    <p class="text-muted text-center mb-4">Login to your Modern Mart account</p>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert alert-danger" role="alert">
        <strong>Error!</strong> <%= error %>
      </div>
    <% } %>

    <form action="/login" method="POST" novalidate>
      <div class="mb-3">
        <label for="email" class="form-label">Email or Admin Username</label>
        <div class="input-group">
          <span class="input-group-text"><i class="fas fa-user"></i></span>
          <input type="text" class="form-control" id="email" name="email" placeholder="Email or Admin Username" required autocomplete="username" />
        </div>
      </div>

      <div class="mb-3">
        <label for="password" class="form-label">Password</label>
        <div class="input-group">
          <span class="input-group-text"><i class="fas fa-lock"></i></span>
          <input type="password" class="form-control" id="password" name="password" placeholder="Password" required autocomplete="current-password" />
        </div>
      </div>

      <button type="submit" class="btn btn-dark w-100 mt-3">Login</button>
      <div class="text-center mt-3">
  <span class="text-muted">Don't have an account?</span>
  <a href="/signup" class="text-decoration-none fw-semibold" style="color: #000;">Sign up</a>
</div>
    </form>
<a href="/" class="d-flex align-items-center justify-content-center mb-4 text-dark text-decoration-none" title="Back to Home">
  <i class="fas fa-home me-2"></i>
  <span>Back to Home</span>
</a>


    <p class="footer-text">© 2024 Modern Mart. All rights reserved.</p>
  </div>
</body>
</html>